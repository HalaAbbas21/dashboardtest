import 'package:flutter/material.dart';
class AppColors{
  static const primary=Color(0xFF78A6C8); //for button and lines in secondary background
  static const secondary =Color(0xFFE9EEF2); // background and the color of word in primary buttons
 static const primaryDark=Color(0xFFE9EEF2); //just for now
 static const secondaryDark=Color(0xFF05141F);//for background in dark
}